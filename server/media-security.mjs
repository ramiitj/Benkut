const signatures={jpeg:[0xff,0xd8,0xff],png:[0x89,0x50,0x4e,0x47],pdf:[0x25,0x50,0x44,0x46],webp:[0x52,0x49,0x46,0x46]};
export const validateMedia=({bytes,declaredType,size,durationSeconds=0,consent=false},policy)=>{if(!consent)throw Error('Explicit capture consent required');if(size>policy.maxUploadBytes)throw Error('Media exceeds upload limit');if(durationSeconds>policy.maxVideoSeconds)throw Error('Media exceeds duration limit');const kind=declaredType==='application/pdf'?'pdf':declaredType.split('/')[1];const sig=signatures[kind];if(!sig||!sig.every((v,i)=>bytes[i]===v))throw Error('Content signature does not match an allowed media type');return{accepted:true,retentionDeadline:new Date(Date.now()+24*60*60*1000).toISOString(),public:false,stripMetadata:true};};
